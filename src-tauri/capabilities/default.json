{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Main window capabilities. Grants the core default set plus window dragging. data-tauri-drag-region invokes the core start_dragging command, which core:window:default does NOT include — without this explicit permission the bar cannot be moved on any platform (macOS or Windows). Resize dragging is deliberately absent: the bar is a fixed 410px layout and the window is not resizable.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:window:allow-start-dragging",
    "updater:default",
    "process:default",
    "dialog:default"
  ]
}

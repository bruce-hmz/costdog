{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Main window capabilities. Grants the core default set plus window dragging. data-tauri-drag-region invokes the core start_dragging command, which core:window:default does NOT include — without this explicit permission the bar cannot be moved on any platform (macOS or Windows).",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:window:allow-start-dragging",
    "core:window:allow-start-resize-dragging"
  ]
}
